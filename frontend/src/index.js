// Importing important libraries and components.
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

// Create a root element to render the app in the HTML file.
const root = ReactDOM.createRoot(document.getElementById('root'));

// Rendering the App component within the root element.
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


